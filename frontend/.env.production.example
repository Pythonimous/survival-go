# Production build: absolute API origin for S3 + CloudFront static hosting.
# Leave empty for same-origin relative paths (Vite dev proxy or nginx /api proxy).
VITE_API_BASE_URL=https://api.example.com
