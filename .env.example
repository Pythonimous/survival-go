# Copy to .env at the repo root (gitignored). Use absolute paths where noted.
# Full reference: docs/development/environment.md

# --- Optional (production-safe defaults shown) ---
SURVIVAL_THRESHOLD=0.95
DEFAULT_TOP_N=8

# --- API abuse limits (optional; see docs/development/environment.md) ---
# API_CREATE_RATE_PER_MINUTE=3
# API_WRITE_RATE_PER_MINUTE=120
# API_MAX_REQUEST_BODY_BYTES=8388608
# MAX_ACTIVE_GAMES_GLOBAL=50
# MAX_ACTIVE_GAMES_PER_IP=5
