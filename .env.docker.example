# Docker Compose environment template. Copy to .env for compose overrides.
# See docs/development/docker-compose.md and docs/development/environment.md

SURVIVAL_THRESHOLD=0.95
DEFAULT_TOP_N=8

# Optional API abuse limits (backend service env in compose)
# API_CREATE_RATE_PER_MINUTE=3
# API_WRITE_RATE_PER_MINUTE=120
# API_MAX_REQUEST_BODY_BYTES=8388608
# MAX_ACTIVE_GAMES_GLOBAL=50
# MAX_ACTIVE_GAMES_PER_IP=5
