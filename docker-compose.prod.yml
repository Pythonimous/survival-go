# Production VM: publish UI on localhost:9080 (local compose uses 8080).
# Caddy on 80/443 reverse-proxies to http://127.0.0.1:9080
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
services:
  frontend:
    ports:
      - "127.0.0.1:9080:80"
